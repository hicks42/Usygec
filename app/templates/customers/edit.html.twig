{% extends 'base.html.twig' %}

{% block title %}Edit Customers
{% endblock %}

{% block content %}
	<div class="d-flex justify-content-between w-100">
		<div>
			<h1>Modifier :
				{{ customer.name }}</h1>
		</div>
		<div>
			{{ include('customers/_delete_form.html.twig') }}
		</div>
	</div>

	{{ include('customers/_form.html.twig', {'button_label': 'Update'}) }}
{% endblock %}
