<form method="post" action="{{ path('app_customers_delete', {'id': customer.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ customer.id) }}">
	<button class="btn btn-danger me-2">
		<i class="fa-solid fa-trash-can"></i>
	</button>
</form>
